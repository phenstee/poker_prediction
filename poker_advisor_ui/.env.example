# Default local backend origin for Vite proxy
VITE_BACKEND_ORIGIN=http://localhost:8000

# Optional absolute API base URL. Leave blank in dev to use /api proxy.
# If this is accidentally set to http://localhost:5173, the app will warn and auto-fallback to /api.
# VITE_API_BASE_URL=
